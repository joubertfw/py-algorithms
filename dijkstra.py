import math as m

class node():
    def __init__(self, nodeId = 0, nodes = [], vextexs = [], visited = False):
        self.nodeId = nodeId
        self.nextNode = nextNode
        self.vextexCost = nextNvextexCostode
        self.visited = visited

# entrada: "a, b, c" onde a = idOrigem, b = idDestino, c= custo de a para b

graph = node(nodeId = 1, vextexCost = 0, visited = False)

for i in input:
    x = node

